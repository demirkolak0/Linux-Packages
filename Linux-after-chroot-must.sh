ln -sf /usr/share/zoneinfo/Europe/Istanbul /etc/localtime && hwclock --systohc && echo "LANG=en_US.UTF-8" > /etc/locale.conf && echo "KEYMAP=trq" > /etc/vconsole.conf && echo "arch" > /etc/hostname && echo "127.0.0.1  localhost" > /etc/hosts && echo "::1  localhost" > /etc/hosts && echo "127.0.1.1 arch.localdomain  arch" > /etc/hosts && pacman -S bash-completion intel-ucode iwd iw wpa_supplicant dialog xdg-user-dirs grub efibootmgr && pacman -Syy && echo "blacklist pcspkr" > /etc/modprobe.d/nobeep.conf
